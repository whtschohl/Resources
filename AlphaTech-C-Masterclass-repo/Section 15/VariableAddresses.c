#include <stdio.h>
#include <stdlib.h>

// Test for the data alignment of "standard variables"

void main()
{
    char a, b, c, d, e, f, g, h;
    short i, j, k, l, m, n;
    int o, p, r, s;
    double x, y, z;

    printf("debug");
}